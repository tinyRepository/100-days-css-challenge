<template>
  <div class="template">
    <div class="container">
      <div class="ball"></div>
      <div
        :class="`blubb blubb-${index + 1}`"
        v-for="(blubb, index) in 8"
        :key="index"
      ></div>
      <div
        :class="`sparkle sparkle-${index + 1}`"
        v-for="(sparkle, index) in 10"
        :key="index"
      ></div>
    </div>
  </div>
</template>

<style lang="scss" scoped>
.template {
  box-shadow: 1px 2px 10px 0px rgba(0, 0, 0, 0.3);
  background: #000;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  filter: contrast(20);
}

.container {
  position: relative;
}

.ball {
  width: 90px;
  height: 90px;
  background: #fff;
  border-radius: 50%;
  filter: blur(5px);
}

@for $i from 1 through 8 {
  .blubb-#{$i} {
    position: absolute;
    top: 20px;
    left: 20px;
    width: 50px;
    height: 50px;
    transform: rotate((random(300)) + deg);

    &:after {
      position: absolute;
      display: block;
      content: '';
      width: 50px;
      height: 50px;
      background: #fff;
      border-radius: 50%;
      transform-origin: (40 - $i * 3) + px (40 - $i * 3) + px;
      animation: rotate (2.5 + $i / 5) + s ease-in-out ($i / 5) + s infinite;
      filter: blur(5px);
    }
  }
}

@for $i from 1 through 10 {
  .sparkle-#{$i} {
    position: absolute;
    top: 38px;
    left: 38px;
    width: (7 + $i) + px;
    height: (7 + $i) + px;
    transform: rotate((random(300)) + deg);

    &:after {
      position: absolute;
      display: block;
      content: '';
      width: (7 + $i) + px;
      height: (7 + $i) + px;
      background: #fff;
      border-radius: 50%;
      transform-origin: (60 - $i * 2) + px (60 - $i * 2) + px;
      animation: rotate (3.5 + $i / 5) + s ease-in-out ($i / 5) + s infinite;
      filter: blur(3px);
    }
  }
}

@keyframes rotate {
  from {
    transform: rotate(0deg) translate3d(0, 0, 0);
  }
  to {
    transform: rotate(360deg) translate3d(0, 0, 0);
  }
}
</style>
